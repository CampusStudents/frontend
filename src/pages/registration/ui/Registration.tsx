import { Button } from "@mui/material";

const Registration = () => {
    return (
        <div>
            <Button>Зарегистрироваться</Button>
        </div>
    );
};

export default Registration;
